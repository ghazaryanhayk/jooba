# Copy to .env and adjust if needed
POSTGRES_USER=jooba
POSTGRES_PASSWORD=jooba
POSTGRES_DB=jooba
POSTGRES_PORT=5432

DATABASE_URL=postgresql://jooba:jooba@db:5432/jooba

API_PORT=8000
WEB_PORT=5173

# Browser calls API via Vite dev proxy; this is for reference / non-proxy usage
VITE_API_URL=http://localhost:8000
